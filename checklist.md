# 📝 Lab 10 Checklist

- [x] Hoàn thành hàm `extract` trong `solution.py` (đọc file JSON, xử lý lỗi `FileNotFoundError`).
- [x] Hoàn thành hàm `validate` trong `solution.py` (lọc `price > 0`, `category` không được rỗng).
- [x] Hoàn thành hàm `transform` trong `solution.py` (tính `discounted_price`, chuẩn hóa `category` thành Title Case, thêm biến `processed_at`).
- [x] Hoàn thành hàm `load` trong `solution.py` (lưu DataFrame ra file `processed_data.csv`).
- [x] Điền thông tin cá nhân (Email, Name) vào file `README.md`.
- [x] Thực hiện báo cáo thử nghiệm (Clean vs Garbage data) vào file `experiment_report.md`.
- [x] Chạy lệnh `python solution.py` hoàn thành không báo lỗi trên local.
- [ ] Commit & Push code lên GitHub để nhận điểm tự động (`git add .`, `git commit`, `git push origin main`).
